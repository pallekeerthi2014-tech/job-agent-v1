# Infrastructure Notes

This directory is reserved for environment-specific infrastructure assets.

- `docker/`: container-related overrides and future helper scripts
- `postgres/`: database bootstrap or tuning assets for later phases

